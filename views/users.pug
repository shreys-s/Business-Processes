extends layout
block content
  script(type='text/javascript' src='/average.js').
  h1 #{title}
  ul.list-group
    each user, i in users
      li.list-group-item
        a(href=''+user._id) #{i+1}. #{user.name}  | #{user.company}
